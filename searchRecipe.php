<?php

/**
 * Le traitement du formulaire se fait ici.
 * Notre algorithme doit faire les choses suivantes :
 * - Récupérer le plat recherché
 * - Vérifier que le plat recherché existe dans notre bd
 * - Si les données sont valides, on affiche un le plat (recette/img etc..)
 */

 if(isset($_POST['recipe'])){
    
 }